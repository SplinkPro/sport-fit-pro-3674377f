## Goal
Hand the code to the client with no visible Lovable / "vibe coding" / GPT-Engineer branding, while keeping the app fully buildable and runnable. Anything that is a real package API or build wiring stays — but renamed/wrapped so client devs see a neutral, professional codebase.

## What I can safely change

### 1. Public-facing & metadata (zero risk)
- **`index.html`** — replace the two `gpt-engineer-file-uploads` social image URLs with a self-hosted image in `/public` (or remove OG image temporarily). Author/title already say SPLINK — fine.
- **`README.md`** — already clean (Pratibha by SPLINK). Will re-verify and add a short "Architecture / Build / Deploy" section so it reads like a normal engineering README, not an AI-generated one.
- **`public/robots.txt`** — verify no Lovable host references.
- **`.plans/plan.md`** — delete or move to `/docs/internal-notes.md` (client-friendly name) or remove entirely.

### 2. Code comments & identifiers (low risk)
- **`src/integrations/lovable/index.ts` & `override.d.ts`** — file lives at this path because it's auto-generated by the platform; the *path* and the `lovable` exported const **must stay** (per your earlier instruction) or the build breaks.
  - What I can change: the header comment, internal variable names where not exported (e.g. `lovableAuth` → `oauthClient`), and remove the "auto-generated" wording so it reads like a normal internal SDK wrapper.
- **`src/pages/Login.tsx`** — `import { lovable }` and `__lovable_token` query param **must stay** (real runtime contract). I can rename the local usage via `import { lovable as authClient }` so the rest of the file reads neutrally, and add a short comment explaining it's the OAuth bridge.
- **`vite.config.ts`** — `lovable-tagger` is a dev-only plugin used at `mode === "development"`. Two options:
  - **(a) Keep it** (it's stripped in production builds anyway, client never sees it at runtime) and rename the import alias: `import { componentTagger as devComponentTagger } from "lovable-tagger"`.
  - **(b) Remove it entirely** from `vite.config.ts` and `package.json` devDependencies — safe because it only adds dev-time DOM data attributes. Recommended for a clean handover.
- **`playwright.config.ts` / `playwright-fixture.ts`** — `lovable-agent-playwright-config` is the test harness. Two options:
  - **(a) Keep but alias** the imports so the file reads `createTestConfig` / `test, expect` with no visible "lovable" word.
  - **(b) Replace** with stock `@playwright/test` config (a few lines). Recommended — removes the last visible reference in test files.

### 3. Package manifest
- **`package.json`** — you originally told me not to touch dependencies. For a real handover I'd recommend:
  - Keep `@lovable.dev/cloud-auth-js` (required for Google OAuth flow).
  - Optionally remove `lovable-tagger` and `lovable-agent-playwright-config` (per options 2b above).
  - Update `name`, `description`, `author`, `repository` fields to SPLINK.
- I will **only** touch dependencies if you confirm — otherwise I leave package.json alone and just alias imports.

### 4. Build & runtime artefacts
- Delete stale `tsconfig.node.tsbuildinfo` (build cache, leaks dep names).
- Add `.gitignore` entries for `*.tsbuildinfo`, `.plans/`, `dist/`.
- Hide the "Edit with Lovable" badge on published deploys via the publish-settings tool.

### 5. Professional polish (optional but recommended)
- Add `LICENSE`, `CONTRIBUTING.md`, `docs/ARCHITECTURE.md` so the repo looks like a maintained engineering project, not a generated one.
- Add a top-of-file header comment block to a handful of entry files (`App.tsx`, `main.tsx`) with copyright + module description.
- Remove any `// TODO: lovable…`, `// generated by…` style comments project-wide.

## What I will NOT touch (per your earlier rule + would break build)
- `package.json` Lovable dep entries (unless you say yes to option 2b/3 above).
- `@lovable.dev/cloud-auth-js` runtime imports (real OAuth SDK).
- The exported `lovable` const name from `src/integrations/lovable/index.ts` (Login.tsx imports it).
- `__lovable_token` query param (real OAuth callback contract).
- The `src/integrations/lovable/` directory location (auto-regenerates here).
- `src/integrations/supabase/client.ts` and `types.ts` (auto-generated).

## Decisions I need from you before coding

1. **`lovable-tagger` in vite.config.ts** — keep (alias only) or remove entirely?
2. **`lovable-agent-playwright-config`** — keep (alias only) or replace with stock `@playwright/test`?
3. **`package.json` dependencies** — am I allowed to remove the two dev-only Lovable packages above? (`@lovable.dev/cloud-auth-js` stays regardless.)
4. **`.plans/` folder** — delete, or move to `docs/internal-notes.md`?
5. **Hide the "Edit with Lovable" badge** on published site? (yes/no)

## Verification after changes
- `bun run build` — must pass.
- `bun run test` — all 117 tests must pass.
- Grep the repo: only allowed remaining references are inside `node_modules`, `bun.lock`, and the unavoidable runtime API names listed above.
- Manual smoke: Google login, Explorer, Analytics, Reports, Badminton module.

Once you answer the 5 questions, I'll execute end-to-end and verify.
